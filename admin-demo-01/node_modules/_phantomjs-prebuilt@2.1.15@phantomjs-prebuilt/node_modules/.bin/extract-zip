#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_extract-zip@1.6.5@extract-zip/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_extract-zip@1.6.5@extract-zip/cli.js" "$@"
  ret=$?
fi
exit $ret
