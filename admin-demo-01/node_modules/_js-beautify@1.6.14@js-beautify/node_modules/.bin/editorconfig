#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_editorconfig@0.13.3@editorconfig/bin/editorconfig" "$@"
  ret=$?
else 
  node  "$basedir/../../../_editorconfig@0.13.3@editorconfig/bin/editorconfig" "$@"
  ret=$?
fi
exit $ret
