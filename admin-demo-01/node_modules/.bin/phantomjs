#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_phantomjs-prebuilt@2.1.15@phantomjs-prebuilt/bin/phantomjs" "$@"
  ret=$?
else 
  node  "$basedir/../_phantomjs-prebuilt@2.1.15@phantomjs-prebuilt/bin/phantomjs" "$@"
  ret=$?
fi
exit $ret
